import { ICredentials } from "data/types/credentials.types";

export const SALES_PORTAL_URL = "http://localhost:8585/#/login";

export const credentials: ICredentials = {
  username: "admin@example.com",
  password: "admin123",
};